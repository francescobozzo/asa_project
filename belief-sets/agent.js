class Agent {
  constructor(id, name, x, y, score) {
    this.id = id;
    this.name = name;
    this.x = x;
    this.y = y;
    this.score = score;
    this.isVisible = true;
  }
}

export default Agent;
